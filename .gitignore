# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next
out
next-env.d.ts

# production build
build
dist

# env files (never commit secrets)
.env
.env.local
.env.*.local

# prisma
prisma/*.db
prisma/*.db-journal

# database data (docker volume mount, if ever bind-mounted locally)
postgres-data

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo

# testing / coverage
coverage

# editor / OS
.vscode
.idea
.DS_Store
Thumbs.db

# vercel (not used, but harmless to ignore if anyone runs `vercel dev`)
.vercel
